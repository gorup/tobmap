[package]
name = "server"
version = "0.0.0"
edition = "2024"

[dependencies]
flatbuffers = "*"
s2 = "*"
schema = { path = "../schema" }
tonic = "*"
prost = "*"
tokio = { version = "*", features = ["macros", "rt-multi-thread"] }

[build-dependencies]
tonic-build = "*"